export default class Song {
  constructor ({songid, mid, songname, singername, albumname, interval}) {
    this.songid = songid
    this.mid = mid
    this.songname = songname
    this.singername = singername
    this.albumname = albumname
    this.interval = interval
  }
}
